<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/styles.css">

</head>
<body>
    <div class="container">
        <div class="contents">
            <div class="row"> 
                <div class="col-3">
                    <img src='https://covers.openlibrary.org/b/isbn/<%= listItems[0].b_values %>-L.jpg' alt="image to be generated" 
                    class="images1" >
                </div>
                <div class="col-9">
                    <h2 class="book-title"><%= listItems[0].title %> By <%= listItems[0].author %></h2>
                    <p>Date Read: <%= listItems[0].dates %></p>
                    <p class="rating">Ratings: <%= listItems[0].rating %>/10</p>
                    <p class="summary"><%= listItems[0].summary %></p>
                </div>
                <div class="Notes">
                    <h2>My Notes</h2>
                    <% for(let item of listItems ) { %>
                        <p>
                            <%= item.notes %>
                        </p>
                    <% } %>
                </div>
                <div class="row">
                    <div class="col-6">
                        <form action="/submit" method="post">
                            <% if (listItems.length > 0) { %>
                                <% if (listItems[0].books_id) { %>
                                    <input type="hidden" name="notes_id" value="<%= listItems[0].books_id %>">
                                <% } else { %>
                                    <input type="hidden" name="notes_id" value="<%= listItems[0].id %>">
                                <% } %>
                                <label for="notes">Enter your notes:</label>
                                <textarea name="notes" id="notes" class="note"></textarea>
                            <% } else { %>
                                <p>No items found.</p>
                            <% } %>

                            
                            <button type="submit" class="btn btn-success">Submit</button>
                        </form>
                    </div>
                    <div class="col-6"></div>

                </div>
                
            </div>
        </div>

    </div>
</body>
</html>